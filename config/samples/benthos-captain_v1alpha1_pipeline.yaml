apiVersion: benthos-captain.benthos.dev/v1alpha1
kind: Pipeline
metadata:
  name: pipeline-sample
spec:
  input:
    test:
      name: foo
      timeout: 10
  pipeline:
    processors:
    - log:
        message: "test"
  output:
    stdout: {}
